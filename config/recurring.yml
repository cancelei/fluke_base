production:
  github_commit_polling:
    class: GithubPollingJob
    queue: default
    schedule: every 60 seconds

  pattern_analysis:
    class: PatternAnalyzerJob
    queue: default
    schedule: every 6 hours

  # clear_solid_queue_finished_jobs:
  #   class: SolidQueue::Jobs::ClearFinishedJobsJob
  #   queue: default
  #   priority: 10
  #   schedule: every day at midnight
  # Example tasks (commented out)
  # periodic_cleanup:
  #   class: CleanSoftDeletedRecordsJob
  #   queue: background
  #   args: [ 1000, { batch_size: 500 } ]
  #   schedule: every hour
  # periodic_command:
  #   command: "SoftDeletedRecord.due.delete_all"
  #   priority: 2
  #   schedule: at 5am every day

development:
  github_commit_polling:
    class: GithubPollingJob
    queue: default
    schedule: every 60 seconds

  pattern_analysis:
    class: PatternAnalyzerJob
    queue: default
    schedule: every 6 hours
