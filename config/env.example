# Copy this file to .env and fill in values as needed.
# Dotenv is enabled in development and test via gem 'dotenv-rails'.

# === LLM Provider API Keys ===
# At least one provider key is required depending on per-project agent provider selection.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# === Optional Networking ===
# HTTP_PROXY=

# === Rails / App Specific (examples) ===
# FLUKE_BASE_DATABASE_USERNAME=postgres
# FLUKE_BASE_DATABASE_PASSWORD=changeme
