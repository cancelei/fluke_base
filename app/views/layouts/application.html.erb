<!DOCTYPE html>
<html data-theme="<%= current_theme %>">
  <head>
    <%# FOWT Prevention: Set theme from localStorage before CSS loads %>
    <script>
      (function() {
        var theme = localStorage.getItem('theme') || <%= current_theme.to_json %>;
        document.documentElement.setAttribute('data-theme', theme);
      })();
    </script>
    <%= render "shared/gtm_head" %>
    <title><%= content_for(:title) || "Fluke Base" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="environment" content="<%= Rails.env %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" type="image/png" sizes="32x32" href="<%= asset_path "logo.png" %>">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= asset_path "logo.png" %>">
    <link rel="apple-touch-icon" sizes="180x180" href="<%= asset_path "logo.png" %>">

    <%# Includes Tailwind CSS v4 with DaisyUI v5 %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# Cloudflare Turnstile script - only load in production if enabled %>
    <% if Rails.env.production? && RailsCloudflareTurnstile.configuration.enabled %>
      <%= cloudflare_turnstile_script_tag %>
    <% end %>
  </head>

  <body class="min-h-screen bg-base-200">
    <%= render "shared/gtm_body" %>
    <!-- DaisyUI Drawer for Mobile Navigation -->
    <div class="drawer">
      <input id="mobile-drawer" type="checkbox" class="drawer-toggle">

      <!-- Main Content Area -->
      <div class="drawer-content flex flex-col">
        <%= render "shared/navbar" %>

        <!-- Toast Notifications Container -->
        <div id="toast-container" aria-live="polite" aria-atomic="true"></div>

        <!-- Flash Messages converted to Toast Notifications -->
        <%= flash_to_toasts %>
        <%= render_toast_flash %>

        <% if user_signed_in? %>
          <%= render "shared/project_context_nav", selected_project: selected_project %>
        <% end %>

        <div id="milestone_bar_container">
          <%= render "shared/milestone_bar" %>
        </div>

        <main class="<%= controller_name == "home" && action_name == "index" ? "" : "mx-auto px-4 sm:px-6 lg:px-8 py-6 w-[90%]" %>">
          <%= yield %>
        </main>
      </div>

      <!-- Mobile Drawer Sidebar - Hidden on desktop (lg+) -->
      <div class="drawer-side z-[9999] max-lg:fixed max-lg:inset-0 lg:hidden" id="mobile-drawer-side">
        <label for="mobile-drawer" aria-label="close sidebar" class="drawer-overlay"></label>
        <%= render "shared/mobile_drawer" %>
      </div>
    </div>

    <%# Theme Modal - Rendered once, controlled by Stimulus %>
    <%= render "shared/theme_modal" %>
  </body>
</html>
