<div class="mx-auto max-w-3xl">
  <div class="px-4 py-5 sm:px-6">
    <h1 class="text-2xl font-bold tracking-tight text-base-content">Edit Milestone</h1>
    <p class="mt-1 text-sm text-base-content/70">
      For project: <%= present(@project).display_name(current_user) %>
    </p>
  </div>

  <%= render "form", milestone: @milestone %>
</div>
