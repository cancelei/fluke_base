<%# Test view for context navbar mobile styling %>
<%# This simulates the logged-in user view for visual testing %>

<%# Mock the context navbar directly %>
<div class="bg-base-200 border-b border-base-300" data-controller="context-nav">
  <div class="mx-auto px-2 sm:px-6 lg:px-8 w-full sm:w-[90%]">
    <div class="context-nav-wrapper">
      <div class="flex items-center justify-between h-11 gap-1 sm:gap-2">
        <%# Left side: Project Selector + Milestone Dropdown %>
        <div class="flex items-center gap-1 flex-shrink min-w-0">
          <%# Project Selector Dropdown %>
          <details class="dropdown min-w-0 flex-shrink" data-context-nav-target="projectDropdown">
            <summary class="btn btn-ghost btn-sm gap-1 sm:gap-2 px-2 sm:px-3 min-w-0">
              <svg class="w-4 h-4 text-base-content/70 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2 6a2 2 0 012-2h5l2 2h5a2 2 0 012 2v6a2 2 0 01-2 2H4a2 2 0 01-2-2V6z" />
              </svg>
              <span class="text-base-content/60 truncate">Select Project</span>
              <svg class="w-4 h-4 text-base-content/60 flex-shrink-0" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
              </svg>
            </summary>
            <ul class="dropdown-content z-[60] menu p-2 shadow-xl bg-base-100 rounded-box w-72 border border-base-200">
              <li><a>New Project</a></li>
              <li><a>Sample Project 1</a></li>
              <li><a>Sample Project 2</a></li>
            </ul>
          </details>
        </div>

        <%# Right side: Placeholder %>
        <div class="text-xs text-base-content/50 flex-shrink hidden sm:block">
          Select a project to get started
        </div>
      </div>
    </div>
  </div>
</div>

<div class="p-4">
  <h1 class="text-xl font-bold mb-4">Context Navbar Mobile Test</h1>
  <p class="text-base-content/70">This page tests the context navbar styling on mobile devices.</p>
  <p class="text-base-content/70 mt-2">The "Select Project" dropdown above should not overflow on mobile.</p>
</div>
