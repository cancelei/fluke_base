<%# Expected locals: project:, current_user: %>
<% presenter = local_assigns[:presenter] || present(project) %>

<li class="px-6 py-5 sm:px-8 hover:bg-gradient-to-r hover:from-blue-50/30 hover:to-purple-50/30 transition-all duration-200">
  <div class="flex items-center justify-between">
    <p class="truncate text-sm font-semibold text-blue-600 hover:text-blue-800 transition-colors">
      <%= link_to presenter.display_name(current_user), project_path(project) %>
    </p>
    <div class="ml-3 flex flex-shrink-0">
      <%= presenter.stage_badge %>
    </div>
  </div>
  <div class="mt-3 sm:flex sm:justify-between">
    <div class="sm:flex">
      <p class="flex items-center text-sm text-gray-600">
        <span><%= presenter.display_description(current_user, truncate: true, length: 100) %></span>
      </p>
    </div>
    <div class="mt-2 flex items-center text-sm text-gray-500 sm:mt-0">
      <p><%= presenter.milestones_summary %></p>
    </div>
  </div>
</li>
