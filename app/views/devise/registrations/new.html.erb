<div class="flex min-h-full flex-col justify-center py-12 sm:px-6 lg:px-8">
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <h2 class="mt-6 text-center text-3xl font-bold tracking-tight">Sign up</h2>
    <p class="mt-2 text-center text-sm text-base-content/70">
      Or
      <%= link_to "sign in if you already have an account", new_session_path(resource_name), class: "link link-primary font-medium" %>
    </p>
  </div>

  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="card bg-base-100 shadow-xl">
      <div class="card-body">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
                     html: { class: "space-y-4", data: { turbo: false } }) do |f| %>
          <%= render "devise/shared/error_messages", resource: %>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <fieldset class="fieldset">
              <%= f.label :first_name, "First name", class: "fieldset-legend" %>
              <%= f.text_field :first_name, autofocus: true, class: "input input-bordered w-full" %>
            </fieldset>

            <fieldset class="fieldset">
              <%= f.label :last_name, "Last name", class: "fieldset-legend" %>
              <%= f.text_field :last_name, class: "input input-bordered w-full" %>
            </fieldset>
          </div>

          <fieldset class="fieldset">
            <%= f.label :email, "Email", class: "fieldset-legend" %>
            <%= f.email_field :email, autocomplete: "email", class: "input input-bordered w-full" %>
          </fieldset>

          <fieldset class="fieldset">
            <%= f.label :github_username, "GitHub Username", class: "fieldset-legend" %>
            <label class="input input-bordered flex items-center gap-2">
              <span class="text-base-content/60">github.com/</span>
              <%= f.text_field :github_username, autocomplete: "github-username", class: "grow border-0 focus:ring-0 bg-transparent", placeholder: "username" %>
            </label>
            <p class="text-xs text-base-content/60 mt-1">Your GitHub username (optional)</p>
          </fieldset>

          <fieldset class="fieldset">
            <%= f.label :github_token, "GitHub Personal Access Token (optional)", class: "fieldset-legend" %>
            <%= f.text_field :github_token, autocomplete: "off", class: "input input-bordered w-full", placeholder: "ghp_..." %>
            <p class="text-xs text-base-content/60 mt-1">Optional. Only needed for private repositories. <a href="https://github.com/settings/tokens/new?scopes=repo&description=FlukeBase" target="_blank" class="link link-primary">Generate token</a> (select 'repo' scope)</p>
          </fieldset>

          <fieldset class="fieldset">
            <%= f.label :password, "Password", class: "fieldset-legend" %>
            <%= f.password_field :password, autocomplete: "new-password", class: "input input-bordered w-full" %>
            <% if @minimum_password_length %>
              <p class="text-xs text-base-content/60 mt-1"><%= @minimum_password_length %> characters minimum</p>
            <% end %>
          </fieldset>

          <fieldset class="fieldset">
            <%= f.label :password_confirmation, "Confirm password", class: "fieldset-legend" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input input-bordered w-full" %>
          </fieldset>

          <%# Turnstile widget - ensure it doesn't overflow on mobile %>
          <div class="max-w-full overflow-hidden">
            <%= render "devise/shared/turnstile_widget" %>
          </div>

          <%# Sticky submit on mobile, normal on desktop %>
          <div class="mt-6 sm:relative sm:bg-transparent sm:border-0 sm:p-0 sm:m-0 form-actions-sticky">
            <%= f.submit "Sign up", class: "btn btn-primary w-full touch-target" %>
          </div>
        <% end %>

        <div class="divider my-6"></div>

        <div>
          <h3 class="text-lg font-semibold mb-4">What's next after signing up?</h3>
          <p class="text-sm text-base-content/70 mb-6">Once you complete your profile, here's how FlukeBase will guide your journey:</p>

          <ul class="steps steps-vertical w-full">
            <li class="step step-primary" data-content="1">
              <div class="text-left">
                <span class="font-medium">Build your profile</span>
                <p class="text-xs text-base-content/60">Tell us about your background, skills, and what you're looking for.</p>
              </div>
            </li>
            <li class="step" data-content="2">
              <div class="text-left">
                <span class="font-medium">Get matched intentionally</span>
                <p class="text-xs text-base-content/60">Based on your goals, we recommend projects and collaborators.</p>
              </div>
            </li>
            <li class="step" data-content="3">
              <div class="text-left">
                <span class="font-medium">Explore opportunities</span>
                <p class="text-xs text-base-content/60">Discover projects, chat with users, and join milestone-driven cycles.</p>
              </div>
            </li>
            <li class="step" data-content="4">
              <div class="text-left">
                <span class="font-medium">Start building — together</span>
                <p class="text-xs text-base-content/60">Work through focused cycles with clear outcomes and aligned incentives.</p>
              </div>
            </li>
          </ul>
        </div>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>
