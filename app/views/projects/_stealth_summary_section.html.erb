<%#
  Stealth Mode Summary Section
  This partial shows the summary when stealth mode is active
%>

<div id="stealth-mode-summary" class="<%= "hidden" unless show_summary %>" data-stealth-mode-target="summary">
  <div class="bg-success/5 backdrop-blur-sm border border-success/30 rounded-2xl p-6 shadow-lg">
    <div class="flex items-start">
      <div class="flex-shrink-0">
        <div class="h-10 w-10 bg-success rounded-full flex items-center justify-center">
          <%= ui_icon(:check, class: "text-success-content") %>
        </div>
      </div>
      <div class="ml-4">
        <h3 class="text-lg font-bold text-success mb-3">Stealth Mode Active</h3>
        <p class="text-sm text-success mb-4 font-medium">
          Your project will be created with privacy-focused default settings while you develop your idea.
        </p>

        <div class="space-y-3">
          <div class="bg-base-100/70 rounded-lg p-4 border border-success/30">
            <h4 class="text-sm font-semibold text-success mb-2">What happens next:</h4>
            <ul class="text-sm text-success space-y-1">
              <li class="flex items-start">
                <%= ui_icon(:check, size: :sm, class: "text-success mt-0.5 mr-2 flex-shrink-0") %>
                Project name: <strong><%= project.name.present? ? project.name : "Generated unique stealth identifier" %></strong>
              </li>
              <li class="flex items-start">
                <%= ui_icon(:check, size: :sm, class: "text-success mt-0.5 mr-2 flex-shrink-0") %>
                Description: <strong><%= project.description.present? ? truncate(project.description, length: 60) : "Professional placeholder text" %></strong>
              </li>
              <li class="flex items-start">
                <%= ui_icon(:check, size: :sm, class: "text-success mt-0.5 mr-2 flex-shrink-0") %>
                Category: <strong><%= project.category.present? ? project.category : "Technology (default)" %></strong>
              </li>
              <li class="flex items-start">
                <%= ui_icon(:check, size: :sm, class: "text-success mt-0.5 mr-2 flex-shrink-0") %>
                Privacy: <strong>All fields private by default</strong>
              </li>
              <li class="flex items-start">
                <%= ui_icon(:check, size: :sm, class: "text-success mt-0.5 mr-2 flex-shrink-0") %>
                Discovery: <strong>Hidden from public explore results</strong>
              </li>
            </ul>
          </div>

          <div class="bg-info/10 rounded-lg p-4 border border-info/30">
            <div class="flex items-center justify-between">
              <p class="text-xs text-info">
                <strong>💡 Tip:</strong> You can customize these defaults above, edit all details after creation, or disable stealth mode anytime to make your project public.
              </p>
              <button type="button"
                      data-action="click->stealth-mode#showFullForm"
                      class="text-xs bg-info text-info-content px-3 py-1.5 rounded-md hover:bg-info/80 transition-colors duration-200 font-medium">
                Show Full Form
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
