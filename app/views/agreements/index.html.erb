<div class="px-4 sm:px-6 lg:px-8">
  <!-- Flash Messages Container -->
  <div id="flash_messages"></div>

  <div class="sm:flex sm:items-center mb-8">
    <div class="sm:flex-auto">
      <h1 class="text-2xl font-semibold leading-6">Agreements</h1>
      <p class="mt-2 text-sm text-base-content/70">
        View and manage your mentorship and co-founder agreements.
      </p>
    </div>
    <div class="mt-4 sm:mt-0 sm:ml-16 sm:flex-none">
      <%= link_to "Back to Projects", projects_path, class: "btn btn-ghost btn-sm gap-2" %>
    </div>
  </div>

  <!-- Filter Controls -->
  <%= render "filters" %>

  <!-- Agreement Results -->
  <div id="agreement_results">
    <%= render "agreement_results" %>
  </div>
</div>
